# PaperScout Agent 开发指引

本文件约束后续在本仓库工作的开发 Agent。开始修改代码前，必须先了解当前实现、真实进度和对应阶段的目标设计。不要把 TODO、计划或简历描述当成已经实现的功能。

## 1. 开始开发前的必读顺序

按以下顺序阅读文档：

1. `docs/architecture-manual.md`
   - 当前代码和运行时架构的权威说明；
   - 用于确认每个文件的职责、数据流、现有边界和已知缺口。
2. `docs/refactor-progress.md`
   - 当前实现状态与验证证据的唯一台账；
   - 用于判断功能是“未开始”“仅契约”“部分实现”还是“已完成”。
3. `docs/todo/project-experience-langgraph-refactor-plan.md`
   - LangGraph 重构的总目标、阶段顺序和验收标准；
   - 这是目标设计，不代表其中能力已经落地。
4. 与当前任务对应的专项设计：
   - Ingest、Wiki、Evidence：`docs/todo/wiki-progressive-loading-plan.md`
   - QA、读取工具、Session、记忆与上下文压缩：`docs/todo/qa-agent-progressive-reading-plan.md`
   - Wiki Review 与 Answer Review：`docs/todo/review-chat-client-plan.md`
5. `README.md`
   - 面向使用者的当前公开能力、配置方法和 Python API。

如果专项设计与总计划冲突，以更新、更具体的专项设计为准，但必须先向用户指出冲突并询问采用哪种解释；确认后同步修订相关文档。

## 2. 文档与用户请求的优先级

- 当前用户请求优先于仓库中的计划和说明文档。
- `docs/todo/` 只描述目标、候选方案和验收标准，不是对当前任务的自动授权。
- 不得因为 TODO 中提到某项功能，就自行扩大当前任务范围、删除数据、调用外部服务或推送远程分支。
- 代码行为、架构手册和进度台账不一致时，不得自行选一个版本继续；先收集证据，再向用户提问。

## 3. 开发方式

每个阶段采用测试先行的最小功能切片：

1. 确认当前阶段、完成标准和不在范围内的内容；
2. 先添加最小 fixture 和关键契约测试；
3. 确认测试能够暴露待实现行为；
4. 只实现当前切片，不顺带重写无关模块；
5. 运行与风险相称的测试；
6. 在同一个功能提交中更新 `docs/refactor-progress.md`；
7. 必要时同步更新 `docs/architecture-manual.md` 和 `README.md`。

进度记录必须写明：实现状态、实现是否已验证、验证命令或人工步骤、剩余缺口和对应提交。类、接口或数据模型存在，不等于执行功能已经完成。

## 4. 必须保持的系统边界

- 项目定位为本地、单用户、单进程工具，除非用户明确要求改变。
- 保留导入、MinerU、不可变 `raw/` 和原子发布边界；核心工作流可按计划重写。
- `raw/` 是事实来源，任何 Agent 都不得修改或删除已有 raw。
- Wiki 是可重建产物；不恢复旧 Wiki、旧 QA、旧 Evidence、迁移兼容或自定义 checkpoint 逻辑。
- Ingest 和 Review 是无工具、无 Session 的 Chat Client；只有宿主负责文件写入和发布。
- QA 只能通过受控只读工具访问项目文件，不能修改 raw 或 Wiki。
- Checkpoint、用户可读 Session 和审计事件是三个独立契约，不能混用。
- 简单前缀预算发生截断时直接失败；不得用不完整 Evidence 生成并发布完整 Wiki。
- 解析质量、Evidence 覆盖、规则校验或 Review 结果不足时必须失败关闭，不发布该论文 Wiki。

## 5. 遇到问题时先向用户提问

出现以下任一情况时，停止有争议的实现或操作，向用户提出一个简洁、具体的问题并等待答复，不要猜测：

- 用户要求、当前代码、架构手册、进度台账或专项设计之间存在冲突；
- 验收标准不清楚，且不同解释会显著改变代码结构、数据格式或最终行为；
- 需要改变本文件第 4 节的系统边界；
- 需要删除、覆盖或迁移已有 raw、Wiki、运行记录、分支或其他用户数据；
- 需要 force push、重写 Git 历史、覆盖远程 main，或当前用户尚未明确授权推送；
- 需要新的凭据、联网服务、付费 API、依赖升级或未确认的 Python/测试环境；
- 测试失败表明问题超出当前功能切片，修复会波及无关模块；
- 发现用户的未提交修改与当前任务重叠，无法安全保留双方改动；
- 无法判断某能力应标记为“部分实现”还是“已完成”。

提问时说明：发现了什么证据、为什么会影响实现、需要用户决定什么。能够由上述必读文档明确回答、且不会改变范围或造成风险的问题，不必重复询问。

## 6. 验证与提交

当前已确认的离线 Python 环境和基础验证命令为：

```powershell
.venv\Scripts\python.exe -m pytest -q
uv --no-cache lock --check
git diff --check
```

- 不得把离线 Mock 测试描述为真实 MinerU、真实 LLM 或端到端语义质量验证。
- 未实现功能不能因为全量测试通过而标记为已完成。
- 验证失败时不得提交或推送为“已完成”；先报告失败和覆盖不足。
- 修改代码后，只有当前用户明确授权并且相关验证通过，才可提交或推送远程 `main`。
- 推送后核对本地与远程提交哈希；无法确认远程 CI 时，在进度台账和交付说明中明确标为待确认。

## 7. 当前推荐的继续方向

以 `docs/refactor-progress.md` 为准选择下一个未完成切片。当前 Ingest 已接入真实 `StateGraph` 和 SQLite Checkpointer；下一优先方向是为独立 Wiki Review Chat Client 编写严格 verdict、语义拒绝、修订循环、最大尝试次数和失败不发布测试，再把 Review 接入现有条件图。不得跳过台账直接宣称完整多 Agent、QA、Session、恢复或双 Review 已完成。
